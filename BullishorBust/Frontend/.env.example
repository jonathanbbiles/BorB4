EXPO_PUBLIC_BACKEND_URL=http://localhost:3000/api
ALPACA_API_KEY=PKN4ICO3WECXSLDGXCHC
ALPACA_SECRET_KEY=PwJAEwLnLnsf7qAVvFutE8VIMgsAgvi7PMkMcCca
ALPACA_BASE_URL=You are a senior JavaScript developer.

In the backend codebase, update the Alpaca API base URL to the following:

ALPACA_BASE_URL=https://api.alpaca.markets

⚠️ Do not change any other environment variables, credentials, logic, or routes.  
Only replace the existing `ALPACA_BASE_URL` value in the `.env` file or equivalent configuration.  
Ensure the new value is correctly loaded by `process.env.ALPACA_BASE_URL` in the backend files (e.g., `index.js`, `trade.js`, `account.js`).

Do NOT modify any file structure, imports, or trade logic.

Only output the modified `.env` file and confirm that the API base URL will resolve to:
https://api.alpaca.markets
ALPACA_DATA_URL=https://data.alpaca.markets/v1beta2
